# Research Log - 2026-06-21

## 2026-06-21-EXP-001 - Poly Live Multi-Strategy Architecture Plan

- Time: 2026-06-21 00:00 CST
- Objective: Fix the production architecture target for adding BTC 5m, ETH 15m, SOL 15m, and BTC
  15m while keeping ETH 5m live.
- Hypothesis: The minimal robust design is one Poly live runtime with strategy-scoped decisions,
  centralized portfolio risk/execution, hot-path-only ordering, and side-path audit/replay.
- Data: Prior PM5M/PM15M research logs, current ETH 5m live config, and current live audit/logging
  structure on Poly.
- Code/config: Documentation only.
- Commands: Not applicable.
- Parameters: See `docs/poly_live_multi_strategy_architecture.md`.
- Outputs: `docs/poly_live_multi_strategy_architecture.md`.
- Metrics: Not applicable.
- Interpretation: Production-readiness work should prioritize `strategy_instance_id`,
  `decision_key_v2`, shared strategy core, central risk/execution, and local replay comparator before
  enabling the four new strategies live.
- Next: Implement Phase 1 schema/config upgrade, with only `eth_5m_main` live-enabled.

## 2026-06-21-EXP-002 - Poly/Local Implementation Details And Strategy Preservation

- Time: 2026-06-21 00:20 CST
- Objective: Convert the architecture plan into concrete Poly-side and local-side implementation
  details while preserving the accepted PM5M and PM15M backtest logic.
- Hypothesis: The minimal implementation path is to keep the order path pure, move replay/audit work
  to side paths, and share strategy kernels between live and replay.
- Data: Current PM5M live `config`, `intent`, `strategy`, and runner structure; PM15M
  `cheap_chip_sweep_15m` accepted backtest logic.
- Code/config: Documentation only.
- Commands: Local source inspection with `sed`/`rg`.
- Parameters: PM5M keeps raw edge 0.20, 60s min-to-end, 1000ms book age, 2000ms reference age, FAK,
  max 3 entries. PM15M keeps direction lock, dynamic edge-only, edge reconfirm, no opposite hedge,
  60s min-to-end, 300ms replay submit latency, and cap2/cap5 instance caps.
- Outputs: Updated `docs/poly_live_multi_strategy_architecture.md`.
- Metrics: Not applicable.
- Interpretation: Live gray sizing may be smaller than research sizing, but signal, direction lock,
  add, edge, and settlement rules must not drift from the accepted backtests.
- Next: Implement schema/config first, then shared PM5M/PM15M strategy kernels.

## 2026-06-21-EXP-003 - Poly Multi-Strategy Runtime Scaffolding And PM15M Shadow Kernel

- Time: 2026-06-21 01:35 CST
- Objective: Continue the Poly-side implementation branch without touching the running live process,
  and move the new 15m strategies from documentation into test-covered shadow runtime code.
- Hypothesis: The next safe step is to support 300s/900s discovery, keep PM15M real order emission
  blocked, and add a PM15M shadow evaluator that matches the accepted direction-lock backtest.
- Data: Branch `codex/poly-multi-strategy-runtime` in
  `/home/hliu/hft_probe_multi_strategy_impl`; prior `cheap_chip_sweep_15m` research logic.
- Code/config: Added `market_duration_seconds` to shadow config and markets; extended Gamma
  discovery for 5m/15m slugs; increased reference retention to 30 minutes; added PM15M
  direction-lock shadow evaluator; routed trader strategy instances by duration; added
  instance-scoped repeat and active-cap enforcement in risk.
- Parameters: PM15M shadow uses raw edge `0.20`, dynamic edge-only limit
  `p_model - raw_edge_threshold`, 60s min-to-end, 1000ms max book age, 2000ms max reference age,
  ETH positive 60s side momentum, conservative SOL positive BTC 60s lead momentum, no SOL NO, and
  per-instance cap2/cap5. PM15M gray/live order emission remains validation-blocked.
- Commands: `cargo fmt --all -- --check`; `cargo test -p pm5m_live_shadow -p pm5m_live_trader
  --no-default-features`; `cargo build --release --bin pm5m-live-trader`; `git diff --check`.
- Outputs: PM15M shadow evaluations now log direction lock state, entry count, dynamic limit,
  cash-sweep metrics, selected count, and reject reasons. Strategy instance IDs and config hashes
  remain attached to evaluation/intent records.
- Metrics: `pm5m_live_shadow` tests `20/20`; `pm5m_live_trader` tests `65/65`; release build
  succeeded.
- Interpretation: This is still pre-deploy code. It improves shadow parity and risk isolation but
  deliberately does not enable real PM15M orders. The live ETH 5m process on Poly was not modified.
- Next: Run secret scan, commit/push the implementation branch, then prepare a minimal-downtime
  deployment plan that starts the new binary/config in shadow before switching any new strategy to
  gray/live.

## 2026-06-21-EXP-004 - Poly Branch Push And Fetch Verification

- Time: 2026-06-21 01:55 CST
- Objective: Fix the current multi-strategy implementation on GitHub and verify Poly can see it
  without interrupting production.
- Hypothesis: A normal branch push plus remote-tracking fetch is enough to preserve the new code
  while leaving the current live worktree and process untouched.
- Data: Local branch `codex/poly-multi-strategy-runtime`; Poly worktree
  `/home/ubuntu/hft_probe_research_pm5m`.
- Code/config: Committed `a3c7d34 Add PM15M shadow runtime and instance risk caps`.
- Commands: `git push origin codex/poly-multi-strategy-runtime`; on Poly, `git fetch origin
  codex/poly-multi-strategy-runtime:refs/remotes/origin/codex/poly-multi-strategy-runtime`;
  `pgrep -af pm5m-live-trader`.
- Outputs: GitHub and local branch both at
  `a3c7d341e95b5c47557a91103e5189fba39bcc49`; Poly remote-tracking ref fetched the same commit.
- Metrics: Production process still running as PID `6986` with the existing live config; Poly
  worktree remains on `dev/logging-sidecar` with only untracked `tmp/`.
- Interpretation: The implementation is safely staged for deployment testing. No live downtime or
  strategy restart occurred.
- Next: Build/deploy from the fetched branch in a separate worktree or staged checkout, run shadow
  first, then plan a short switch window only after log parity checks.

## 2026-06-21-EXP-005 - Pre-Live Cutover Audit And Reconciliation Hardening

- Time: 2026-06-21 18:30 CST
- Objective: Re-check strategy logic, order emission, risk gates, execution reports, audit logs, and
  local replay/reconciliation readiness before switching the multi-strategy runtime toward live.
- Hypothesis: The runtime is close to live-ready, but the final audit should catch any mismatch
  between accepted backtest semantics and live implementation before capital is at risk.
- Data: Branch `codex/poly-multi-strategy-runtime` in
  `/home/hliu/hft_probe_multi_strategy_impl`; accepted PM15M EXP-006 buy-count-cap definition in
  `docs/research_logs/2026-06-20.md`; current local replay/alignment docs.
- Code/config:
  - Removed PM15M strategy-local attempt counting from the live runner. EXP-006 caps count successful
    filled execution groups; live now leaves count caps to `RiskGate`, where unfilled orders release
    slots and filled/partial executions count.
  - Removed the remaining `post_latency_ms` effect on consecutive submit-failure risk. POST latency is
    now telemetry/audit only.
  - Added live validation for every gray/live strategy instance effective `buy_amount_usdc >= 1.0`.
  - Added strategy-scoped join keys to `shadow_execution_check` records.
  - Added `scripts/compare_multi_strategy_runs.py` for side-path comparison by
    `strategy_instance_id + decision_key_v2`.
  - Updated risk docs to the current `catastrophe_drawdown_usdc` derived soft/hard/catastrophe
    stress-loss model.
- Commands:
  `cargo fmt --all -- --check`;
  `git diff --check`;
  `cargo test -p pm5m_live_shadow -p pm5m_live_trader --no-default-features`;
  `cargo build --release --bin pm5m-live-trader`;
  `pytest -q tests/test_live_executor.py tests/test_pm5m_policy_replay.py tests/test_event_time_replay.py`;
  `python3 -m py_compile scripts/compare_multi_strategy_runs.py scripts/compare_eth5m_shadow.py`.
- Metrics: Rust tests passed: `pm5m_live_shadow 20/20`, `pm5m_live_trader 65/65`; Python tests
  passed `35/35`; release build succeeded; comparator empty-run smoke test succeeded.
- Interpretation: The code path is healthier for a tightly capped small-live cutover than it was
  before this audit. The main remaining operational step is deliberate config promotion to live/gray
  with operator confirmation; this audit did not switch live orders on.
- Next: Prepare the exact live config diff, run it in the intended container, and use the
  multi-strategy comparator plus local recorded data to review any live/replay mismatches.

## 2026-06-21-EXP-006 - Documentation State Sync

- Time: 2026-06-21 19:40 CST
- Objective: Scan implementation-side and local research-side documentation and align active docs
  with the latest multi-strategy runtime state.
- Hypothesis: The main documentation risk is stale plan language around live state, decision keys,
  risk tiers, and local reconciliation tooling.
- Data: `/home/hliu/hft_probe_multi_strategy_impl` docs, `/home/hliu/hft_runtime/docs`, and current
  branch status.
- Code/config: Documentation only.
- Commands: `rg` scans over README, handoff, config-debt inventory, architecture docs, and research
  logs.
- Outputs:
  - Updated the implementation README and handoff to identify Rust `pm5m-live-trader` as the current
    code path, with Poly production still on the old single ETH 5m config.
  - Updated the multi-strategy architecture doc to separate target architecture from current branch
    status.
  - Updated `decision_key_v2` wording to the current
    `{family_key}:{strategy_instance_id}:{symbol}:{bar_open_time_ms}:{condition_id}:{outcome}:{repeat_index}`
    format, with reconciliation by `strategy_instance_id + decision_key_v2`.
  - Updated risk wording to the current `catastrophe_drawdown_usdc` derived soft/hard/catastrophe
    stress-loss model and noted that `scripts/compare_multi_strategy_runs.py` is the implemented
    comparator today.
- Metrics: No code changed in this experiment.
- Interpretation: Active docs now reflect that multi-strategy code is staged but not live-cutover,
  while historical research entries remain append-only even when they mention superseded field
  names.
- Next: Before actual live cutover, create the exact production config diff and run the comparator
  against the cutover/shadow artifacts.

Addendum:

- Linked the root README and PM5M start document to the Poly live multi-strategy architecture doc.
- Clarified that the current ETH gray PM5M backtest config is the local replay baseline, not the
  five-strategy Poly cutover config.
